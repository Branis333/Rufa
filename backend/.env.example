# Application environment: development, test, or production
NODE_ENV=development

# Port used by the HTTP server
PORT=3000

# Allowed browser origin. Use * while developing without a frontend URL.
CORS_ORIGIN=*
